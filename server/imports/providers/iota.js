import IOTA from "iota.lib.js";

/*let iota = new IOTA({
    'host': 'https://node.iota-tangle.io',
    'port': 14265
});

// Call the 'getNodeInfo call to check that the node is working
iota.api.getNodeInfo((error, success) => {
  if (error) {
    console.log(error)
  } else {
    console.log("Iota: node listener initialized")
  }
});*/
